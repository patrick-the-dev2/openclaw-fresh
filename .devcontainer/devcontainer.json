{
  "name": "OpenClaw Free",
  "image": "node:22-bullseye",
  "features": {
    "ghcr.io/devcontainers/features/git:1": {}
  },
  "postCreateCommand": "npm ci",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.vscode-typescript-next"
      ]
    }
  },
  "forwardPorts": [8080, 18789],
  "remoteUser": "node",
  "secrets": {
    "OPENROUTER_API_KEY": {
      "description": "OpenRouter API key (free tier available)"
    },
    "NVIDIA_NIM_API_KEY": {
      "description": "NVIDIA NIM API key (free at build.nvidia.com)"
    },
    "OPENCLAW_GATEWAY_TOKEN": {
      "description": "Gateway token (generate with: openssl rand -hex 32)"
    }
  }
}
